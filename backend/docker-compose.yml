services:
  db-local:
    platform: linux/x86_64
    image: postgres:17
    volumes:
      - ./compose_data/db-local:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=db-local
    ports:
      - 5490:5432
